// utils/passport.ts
import passport from 'passport';
import './accessTokenStrategy'; // Importing the access token strategy
import './refreshTokenStrategy'; // Importing the refresh token strategy

export default passport;
