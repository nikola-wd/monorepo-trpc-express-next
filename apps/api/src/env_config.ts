const MILISECONDS_IN_A_SECOND = 1000;

export const env_config = {
  // session_max_duration: 10 * MILISECONDS_IN_A_SECOND, // 10 seconds
  SESSION_MAX_DURATION: 10 * MILISECONDS_IN_A_SECOND * 60 * 6, // 1 hour
};
