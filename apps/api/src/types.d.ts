// types.d.ts
declare namespace Express {
  interface Request {
    user?: any; // Adjust the type as necessary
    // currentUser?: any; // Adjust the type as necessary
  }
}

// TODO: ADd correct type ^
